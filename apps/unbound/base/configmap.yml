apiVersion: v1
kind: ConfigMap
metadata:
  name: unbound-config
data:
  unbound.conf: |
    server:
      verbosity: 1
      interface: 0.0.0.0
      port: 53
      do-ip4: yes
      do-udp: yes
      do-tcp: yes
      access-control: 0.0.0.0/0 allow
    cachedb:
      backend: "redis.dns.svc.cluster.local:6379"
      redis-server-host: redis
      redis-server-port: 6379
      redis-expire-records: yes
